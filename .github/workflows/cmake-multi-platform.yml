name: CMake Multi-Platform

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: 'recursive'
    
    - name: Install Linux dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          cmake \
          libsdl2-dev \
          libsdl2-image-dev \
          libsidplayfp-dev \
          pkg-config
    
    - name: Install macOS dependencies
      if: runner.os == 'macOS'
      run: |
        brew update
        brew install cmake sdl2 sdl2_image sidplayfp pkg-config
    
    - name: Install Windows dependencies
      if: runner.os == 'Windows'
      shell: powershell
      run: |
        # Install SDL2
        Invoke-WebRequest -Uri "https://www.libsdl.org/release/SDL2-devel-2.32.8-VC.zip" -OutFile "SDL2-devel.zip"
        Expand-Archive -Path SDL2-devel.zip -DestinationPath .
        $sdl2Path = (Resolve-Path "SDL2-*").Path
        # SDL2_DIR doit pointer vers le répertoire cmake/ qui contient SDL2Config.cmake
        $sdl2ConfigDir = Join-Path $sdl2Path "cmake"
        if (-not (Test-Path $sdl2ConfigDir)) {
          Write-Host "Warning: cmake/ directory not found in SDL2, using root directory"
          $sdl2ConfigDir = $sdl2Path
        }
        Write-Host "SDL2_DIR will be set to: $sdl2ConfigDir"
        Write-Host "CMAKE_PREFIX_PATH will be set to: $sdl2Path"
        "SDL2_DIR=$sdl2ConfigDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
        "CMAKE_PREFIX_PATH=$sdl2Path" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
    
    - name: Configure CMake
      run: cmake -B ${{ github.workspace }}/build -S ${{ github.workspace }}
      env:
        SDL2_DIR: ${{ env.SDL2_DIR }}
        CMAKE_PREFIX_PATH: ${{ env.CMAKE_PREFIX_PATH }}
    
    - name: Build
      run: cmake --build ${{ github.workspace }}/build --config Release
